// src/array/minBy.ts
function minBy(items, getValue) {
  let minElement = items[0];
  let min = Infinity;
  for (const element of items) {
    const value = getValue(element);
    if (value < min) {
      min = value;
      minElement = element;
    }
  }
  return minElement;
}

export {
  minBy
};
//# sourceMappingURL=chunk-IJD7FE2Z.mjs.map