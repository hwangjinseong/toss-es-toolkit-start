// src/array/partition.ts
function partition(arr, isInTruthy) {
  const truthy = [];
  const falsy = [];
  for (const item of arr) {
    if (isInTruthy(item)) {
      truthy.push(item);
    } else {
      falsy.push(item);
    }
  }
  return [truthy, falsy];
}

export {
  partition
};
//# sourceMappingURL=chunk-DN3NOZ3H.mjs.map