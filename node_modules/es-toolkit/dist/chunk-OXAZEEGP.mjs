// src/array/uniqBy.ts
function uniqBy(arr, mapper) {
  const map = /* @__PURE__ */ new Map();
  for (const item of arr) {
    const key = mapper(item);
    if (!map.has(key)) {
      map.set(key, item);
    }
  }
  return Array.from(map.values());
}

export {
  uniqBy
};
//# sourceMappingURL=chunk-OXAZEEGP.mjs.map