// src/array/fill.ts
function fill(arr, value, start = 0, end = arr.length) {
  start = Math.max(start, 0);
  end = Math.min(end, arr.length);
  for (let i = start; i < end; i++) {
    arr[i] = value;
  }
  return arr;
}

export {
  fill
};
//# sourceMappingURL=chunk-C4WE4GLC.mjs.map